name: IaC

on:
  workflow_dispatch:     
  pull_request:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    # - name: Generate Dockerfile dynamically
    #   run: |
    #     cat << 'EOF' > Dockerfile.${{ github.run_id }}
    #     # 动态生成的 Dockerfile
    #     FROM mirror.ccs.tencentyun.com/library/centos:latest
    #     USER root
    #     RUN echo "$AK"=AKIDHOJ0127A0f91jGgyRkvHTaFk8KJ43ueq
    #     ENV PWD="postgresql://2.1.1.1:xxx@yyy"
    #     ENV RSYNC_PASSWORD="TPcloud@123"
    #     RUN echo "root:123456" | chpasswd
    #     COPY virus .
    #     ENV ROOTPASSWD=${{ github.run_id }}
    #     EOF
  
    #     echo "生成的 Dockerfile 内容:"
    #     cat Dockerfile.${{ github.run_id }}
    #     cp Dockerfile.${{ github.run_id }} /home/test/Dockerfile.${{ github.run_id }}
                
    - name: CICD Scan
      run: |
        docker run --rm -v /home/test/iacfile:/home/test ccr.ccs.tencentyun.com/yunjing_agent/cicdscanner:5.3.0.30 --token=0be94e16-97db-4f31-8446-4faa15c747f2 --task iac --iacPath /home/test --debugMode=true
